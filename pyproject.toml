[project]
name = "edge-iq"
version = "0.6.0a1"
description = "Local-first NFL player-prop research, validation, and paper-trading analytics"
readme = "README.md"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "--strict-markers"
