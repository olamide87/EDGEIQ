# Experiments

Place reproducible experiment scripts and small machine-readable report schemas here.
Generated reports belong in ignored `research/reports/`. No learned-model experiment
starts until v0.5A–C promotion gates are satisfied.

`run_v05b_feature_audit.py` is a deterministic data-quality audit, not a model
experiment. It uses only committed offline fixtures and writes ignored output.
